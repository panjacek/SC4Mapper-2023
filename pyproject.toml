[tool.black]
line-length = 120

[tool.isort]
profile = "black"

[tool.flake8]
max-line-length = 120

[tool.ruff]
line-length = 120

[tool.coverage.run]
source = ["sc4_mapper"]

[tool.pytest.ini_options]
addopts = "--cov=sc4_mapper --cov-report term-missing"
